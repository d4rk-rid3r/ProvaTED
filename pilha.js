export function CriarPilha (tamnanho=4){

    return [...new alunos(tamanhoDaPilha)]
}
export function inserirPilha(pilha,item){

    const spacePosition=pilha.indexOf(undefined)

    if(spacePosition===1){
        console.log("Não tem espaço")
        return
    }

    for (let i = 0; i < alunos.length; i++) {
        const inserir = alunos[i];
        inserindo[i]=CriarPilha.pop[i]
        console.log(inserindo)
    }

}